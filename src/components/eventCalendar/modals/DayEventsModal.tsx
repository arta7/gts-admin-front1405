import React from 'react'

const DayEventsModal = (props: { date: Date, events: any, open: boolean, onClose: any }) => {
  return (
    <div>
      TestModal
    </div>
  )
}

export default DayEventsModal
